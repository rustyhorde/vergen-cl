[package]
name = "vergen-cl"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.79"
vergen-pretty = { version = "0.3.1", features = ["color"] }

[build-dependencies]
anyhow = "1.0.79"
vergen = { version = "8.2.10", default-features = false, features = [
    "build",
    "cargo",
    "git",
    "gitoxide",
    "rustc",
    "si"
] }